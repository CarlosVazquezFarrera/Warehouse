import { CompleteStockPipe } from './complete-stock.pipe';

describe('CompleteStockPipe', () => {
  it('create an instance', () => {
    const pipe = new CompleteStockPipe();
    expect(pipe).toBeTruthy();
  });
});

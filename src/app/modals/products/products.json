{
    "errors": {
        "presentationQuantity": {
            "required": "Quantity per presentation is required",
            "min": "Must be a higher number"
        },
        "formatQuantity": {
            "required": "Quantity per productFormat is required",
            "min": "Must be at least one"
        },
        "supplierPart": {
            "required": "SupplierPart is required"
        },
        "name": {
            "required": "Name is required"
        }
    }
}